{
  "name": "@firehose-data-service/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "GRC-006 Mainline data service contracts (FirehoseDataService).",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "fmt": "forge fmt",
    "fmt:check": "forge fmt --check",
    "update-horizon": "./scripts/update-horizon.sh",
    "export-abi": "jq '.abi' out/FirehoseDataService.sol/FirehoseDataService.json > ../subgraph/abis/FirehoseDataService.json"
  },
  "devDependencies": {
    "@graphprotocol/horizon": "^1.1.1",
    "@graphprotocol/interfaces": "^0.6.6"
  }
}
